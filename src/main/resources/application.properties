# Database Configuration
# Toggle between H2 (dev) and PostgreSQL (prod)

# H2 Database
db.url=jdbc:h2:./lms_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
db.user=sa
db.pass=
db.init=true

# PostgreSQL Database (example)
# db.url=jdbc:postgresql://localhost:5432/lms
# db.user=postgres
# db.pass=password
# db.init=false

# Fine Calculation
fine.rate.per.day=0.50
